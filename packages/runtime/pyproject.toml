[project]
name = "persona-runtime"
version = "1.1.0"
description = "Generation loop, router, and agentic engine for persona-core."
readme = "README.md"
requires-python = ">=3.11"
# MIT — the open-source engine (Spec 33). Free for any use, including
# commercial. The application layer (persona-api / persona-web) is separately
# licensed PolyForm-Noncommercial-1.0.0 (source-available, noncommercial).
license = "MIT"
license-files = ["LICENSE"]
authors = [
    { name = "Yasin Hessnawi", email = "yasinhessnawi1@gmail.com" },
]
keywords = [
    "ai",
    "persona",
    "agent",
    "llm",
    "agentic",
    "router",
    "tool-use",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: POSIX",
    "Operating System :: MacOS",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Typing :: Typed",
]
dependencies = [
    "persona-core",
    # Direct dep for prompt-token estimation in prompt.py (D-05-8). Already
    # transitive via persona-core (D-01-11); declared directly per
    # ENGINEERING_STANDARDS.md §5 ("declare directs, audit transitives").
    "tiktoken>=0.7,<1",
    # R6 crisis-detection encoder (R6-D-1): the few-shot LogisticRegression head over
    # the frozen multilingual-MiniLM body. Already transitive via sentence-transformers
    # (persona-core); declared directly per the "declare directs" rule. The frozen body
    # reuses persona-core's shipped sentence-transformers dep (no new serving stack).
    "scikit-learn>=1.3,<2",
    # R6: the bundled few-shot training set is YAML (already a transitive dep, declared
    # direct because crisis_encoder.py imports it at module load).
    "pyyaml>=6,<7",
]

[project.urls]
Homepage = "https://github.com/yasinhessnawi1/Open-Persona"
Repository = "https://github.com/yasinhessnawi1/Open-Persona"
Issues = "https://github.com/yasinhessnawi1/Open-Persona/issues"
Changelog = "https://github.com/yasinhessnawi1/Open-Persona/blob/main/CHANGELOG.md"

[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/persona_runtime"]
include = [
    "src/persona_runtime/py.typed",
    # R6: the bundled crisis-encoder few-shot training set, loaded at head-fit time
    # via importlib.resources. Ship it in the wheel.
    "src/persona_runtime/data/*.yaml",
]

[tool.uv.sources]
persona-core = { workspace = true }
